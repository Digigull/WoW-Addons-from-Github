__pycache__/
*.py[cod]
# PyInstaller output, for when the Windows build lands
build/
dist/
*.spec

# The build script's own virtualenv for python-appimage.
.build-venv/

# Console devices on Windows, ordinary filenames everywhere else. Created by
# accident, they are reserved names that make `git checkout` refuse the whole
# repository on Windows.
CONOUT$
CONIN$
